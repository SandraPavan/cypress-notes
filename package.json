{
  "name": "cypress-notes",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:e2e": "npx cypress run --spec 'cypress/e2e/**/*.spec.js' --reporter mochawesome",
    "test:api": "npx cypress run --spec 'cypress/api/**/*.spec.js' --reporter mochawesome",
    "test:all": "npx cypress run --spec 'cypress/e2e/**/*.spec.js,cypress/api/**/*.spec.js' --reporter mochawesome"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cypress": "^13.15.0",
    "dotenv": "^16.4.5",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
